# Many / Recent / Examples

- [DECISION] because many of the most recent examples were technically flawed (GPT-4 API, gRPC keepalive, IAM role setup, ECS deployment, E2EE examples) → next agent should validate and replace those patterns before proposing production code
