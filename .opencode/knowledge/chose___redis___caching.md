# Chose / Redis / Caching

- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] kept Redis caching for resized images because it reduced image processing time from `800ms` to `200ms`
- [DECISION] kept Redis caching because it reduced image processing time from `800ms` to `200ms`
- [DECISION] kept Redis caching because it reduced image processing time from `800ms` to `200ms`
- [DECISION] kept Redis caching because it reduced image processing time from `800ms` to `200ms`
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching design with `allkeys-lru`, `2GB`, and TTL settings because API latency and cache hit rate were recurring optimization priorities
- [DECISION] chose Redis caching for resized images because it reduced processing time from `800ms` to `200ms`
- [DECISION] chose Redis caching for resized images because it reduced processing time from `800ms` to `200ms`
- [DECISION] kept Redis caching for resized images because it reduced image processing time from `800ms` to `200ms` | supersedes:previous_ref
