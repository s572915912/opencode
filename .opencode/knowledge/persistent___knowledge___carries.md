# Persistent / Knowledge / Carries

- [CONFLICT] persistent knowledge still carries an older “latest active thread” around `backtest.py` / `BacktestModule`, but the newest local branch here is live trading API + dashboard + monitoring performance. Status: UNRESOLVED — ask the user which thread to resume if they do not specify.
