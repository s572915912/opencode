# Improved / Latency / Redis

- [UPDATE] `improved_latency_with_redis: 210ms (was: 320ms)` — user explicitly reported this improvement after caching diffusion features in Redis | supersedes:measured_latency_only
