# Total / Frame / Latency

- [DECISION] Because total frame latency with counting enabled is already `180ms`, under the `under 250ms per frame` target → next optimization should focus on cleaner data structures and minimal overlay overhead, not drastic architecture change.
- [DECISION] Because total frame latency with counting enabled is already `180ms`, under the `under 250ms per frame` target → the next optimization should focus on cleaner data structures and low-overhead rendering, not drastic architectural change.
