# Previous / Assistant / Examples

- [DECISION] Because previous assistant examples often used insecure login SQL like `SELECT * FROM users WHERE username = $1 AND password = $2` or string interpolation → next agent should correct toward hashed-password verification and parameterized queries only
- [DECISION] Because previous JWT examples mixed access tokens and refresh tokens incorrectly → next agent should clearly separate access-token signing/verification from refresh-token signing/verification
- [DECISION] Because previous assistant examples suggested client-side JWT verification patterns that may be inappropriate for real auth enforcement → next agent should distinguish UI decoding from backend trust/verification
- [DECISION] Because previous assistant examples often used insecure login SQL like `SELECT * FROM users WHERE username = $1 AND password = $2` or string interpolation → next agent should correct toward hashed-password verification and parameterized queries only
- [DECISION] Because previous JWT examples mixed access tokens and refresh tokens incorrectly → next agent should clearly separate access-token signing/verification from refresh-token signing/verification
- [DECISION] Because previous assistant examples suggested client-side JWT verification patterns that may be inappropriate for real auth enforcement → next agent should distinguish UI decoding from backend trust/verification
- [DECISION] Because previous assistant examples often used insecure login SQL like `SELECT * FROM users WHERE username = $1 AND password = $2` or string interpolation → next agent should correct toward hashed-password verification and parameterized queries only
- [DECISION] Because previous JWT examples mixed access tokens and refresh tokens incorrectly → next agent should clearly separate access-token signing/verification from refresh-token signing/verification
- [DECISION] Because previous assistant examples suggested client-side JWT verification patterns that may be inappropriate for real auth enforcement → next agent should distinguish UI decoding from backend trust/verification
- [FACT] many immediately preceding assistant examples in the latest continuation history were technically flawed and should not be treated as implementation artifacts, especially around GPT-5, gRPC, MFA, X-Ray, blue-green deployment, webhook analytics, React Suspense, and Redis fallback cache code
- [DECISION] because many immediately preceding assistant snippets in this segment used invalid/mismatched model classes and APIs (for example DistilBert for distilgpt2, incorrect AMP advice, and text-only models pretending to accept image features) → the next agent should replace those examples with validated **PyTorch 1.13** / **Transformers v4.29** patterns before continuing
- [DECISION] because many immediately preceding assistant snippets in this segment used invalid/mismatched model classes and APIs (for example DistilBert for distilgpt2, incorrect AMP advice, and text-only models pretending to accept image features) → the next agent should replace those examples with validated **PyTorch 1.13** / **Transformers v4.29** patterns before continuing
- [FACT] some previous assistant snippets across OpenCV, TensorRT, Flask-SocketIO, ECS, API Gateway, ALB, Grafana, and CodeDeploy were technically invalid and should not be treated as implementation artifacts
- [FACT] Some previous assistant snippets across OpenCV, TensorRT, Flask-SocketIO, ECS, API Gateway, ALB, Grafana, and CodeDeploy were technically invalid and should not be treated as implementation artifacts
- [FACT] Some previous assistant snippets across OpenCV, TensorRT, Flask-SocketIO, ECS, API Gateway, ALB, Grafana, and CodeDeploy were technically invalid and should not be treated as implementation artifacts
- [FACT] Some previous assistant snippets across OpenCV, TensorRT, Flask-SocketIO, ECS, API Gateway, ALB, Grafana, and CodeDeploy were technically invalid and should not be treated as implementation artifacts
- [FACT] Some previous assistant snippets across OpenCV, TensorRT, Flask-SocketIO, ECS, API Gateway, ALB, Grafana, and CodeDeploy were technically invalid and should not be treated as implementation artifacts
- [FACT] Some previous assistant snippets across OpenCV, TensorRT, Flask-SocketIO, ECS, API Gateway, ALB, Grafana, and CodeDeploy were technically invalid and should not be treated as implementation artifacts
- [DECISION] Because previous assistant examples often used insecure login SQL and mixed access/refresh token semantics → next agent should correct toward hashed-password verification, parameterized SQL, and strict separation of access-token and refresh-token handling.
- [DECISION] Because previous assistant examples often used insecure auth SQL like `SELECT * FROM users WHERE username = $1 AND password = $2` or string interpolation → next agent should correct toward hashed-password verification and parameterized queries only.
- [DECISION] Because previous JWT examples mixed access tokens and refresh tokens incorrectly → next agent should clearly separate access-token signing/verification from refresh-token signing/verification.
- [DECISION] Because previous assistant examples suggested frontend handling that tried to read/set `HttpOnly` cookies from JavaScript → next agent should explicitly correct that.
- [DECISION] because previous assistant examples often used insecure auth SQL and mixed access/refresh semantics → next agent should correct toward hashed-password verification, parameterized queries only, and strict separation of access-token signing/verification from refresh-token signing/verification
- [DECISION] because previous assistant examples suggested frontend handling that tried to read/set `HttpOnly` cookies from JavaScript → next agent should explicitly correct that
- [DECISION] because many immediately preceding assistant responses in the newest segment were technically weak (OpenAI/GPT-4 endpoints, gRPC keepalive, IAM roles, ECS workflows, E2EE, cache patterns) → next agent should validate and replace those patterns before proposing production code
- [DECISION] because many immediately preceding assistant responses in the newest segment were technically weak (GPT-5, gRPC, MFA, X-Ray, blue-green deployment, webhook analytics, Redis fallback cache) → next agent should validate and replace those patterns before proposing production code
- [FACT] many immediately preceding assistant snippets in this captioning thread used invalid or mismatched APIs and should not be treated as final implementation artifacts
- [DECISION] because many immediately preceding assistant snippets in this segment used invalid/mismatched model classes and APIs → future continuation should correct them with validated `PyTorch 1.13` / `Transformers v4.29` patterns before continuing
- [DECISION] because many immediately preceding assistant snippets in this captioning thread and later branches used invalid or mismatched APIs → future continuation should replace them with validated implementations rather than extend them
- [DECISION] because many immediately preceding assistant snippets in this captioning thread and later branches used invalid or mismatched APIs → future continuation should replace them with validated implementations rather than extend them
- [DECISION] because many immediately preceding assistant snippets in this captioning thread and later branches used invalid or mismatched APIs → future continuation should replace them with validated implementations rather than extend them
- [DECISION] Because previous assistant examples across this chatbot/langdetect thread were often technically weak → next agent should correct JWT refresh-token flow carefully, including:
- [DECISION] Because previous assistant examples across this chatbot/langdetect thread were often technically weak → next agent should correct JWT refresh-token flow carefully, including:
- [DECISION] Because previous assistant examples often used insecure login SQL and mixed access/refresh token semantics → next agent should correct toward hashed-password verification, parameterized queries only, and strict access-token / refresh-token separation
- [DECISION] Because previous assistant examples suggested frontend handling that tried to read/set `HttpOnly` cookies from JavaScript → next agent should explicitly correct that
