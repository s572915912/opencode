# Api / Throughput / Reported

- [FACT] `api_throughput_reported: 150 requests per second` under Locust `2.15` load testing is newer than the older `100 concurrent users` example and should be retained separately
