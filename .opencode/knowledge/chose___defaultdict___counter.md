# Chose / Defaultdict / Counter

- [DECISION] chose `defaultdict` / `Counter` patterns because manual dictionary initialization caused issues like `KeyError: 17` and redundant logic
- [DECISION] chose `defaultdict` / `Counter` patterns because manual dictionary initialization caused issues like `KeyError: 17` and redundant logic.
- [DECISION] chose `defaultdict` / `Counter` because manual dictionary initialization caused issues like `KeyError: 17`.
- [DECISION] chose `defaultdict` / `Counter` because manual dictionary initialization caused issues like `KeyError: 17` and redundant branching.
- [DECISION] Because manual dictionary initialization caused issues like `KeyError: 17` and user is new to dictionary-based counting → next implementation should likely use `defaultdict(int)` or `Counter` and separate counting state from overlay rendering.
- [DECISION] chose `defaultdict` / `Counter` because manual dictionary initialization caused issues like `KeyError: 17`
- [DECISION] chose `defaultdict(int)` / `Counter` because manual dictionary initialization caused issues like `KeyError: 17` and redundant branching
