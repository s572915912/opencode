# Chose / June / August

- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
- [DECISION] chose `June 10` and `August 10` because moving initial feature extraction to `April 20` reduced the buffer too much and the user wanted enough time so things “still fit and isn't too rushed”
