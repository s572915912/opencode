# Chose / Composite / Index

- [DECISION] chose a composite index on `(post_time, status)` because DB query time was a significant bottleneck and it reduced query time from `400ms` to `120ms`
- [DECISION] chose a composite index on `(post_time, status)` because it reduced DB query time from `400ms` to `120ms`
- [DECISION] chose a composite index on `(post_time, status)` because it reduced DB query time from `400ms` to `120ms`
- [DECISION] chose a composite index on `(post_time, status)` because DB query time was a significant bottleneck | supersedes:previous_ref
