# Likely / Dependency / Injection

- [DECISION] likely need dependency injection for `io` / shared services because the refactor introduced circular dependency risk and `"Maximum call stack size exceeded"`
